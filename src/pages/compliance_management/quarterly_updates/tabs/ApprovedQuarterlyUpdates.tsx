import React from 'react';
import { Box, Typography } from '@mui/material';

const ApprovedQuarterlyUpdates: React.FC = () => {
    return (
        <Box sx={{ mt: 2 }}>
            <Typography variant="h6" gutterBottom>Approved Quarterly Updates</Typography>
            <Typography>Content for approved quarterly updates will go here.</Typography>
        </Box>
    );
};

export default ApprovedQuarterlyUpdates; 